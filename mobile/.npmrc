# React Native / Expo dependency trees frequently trip npm's strict peer-dep
# resolver (ERESOLVE) even though the versions are the SDK-correct set. This
# relaxes that check, matching how `expo install` resolves packages.
legacy-peer-deps=true
