# Optional: install alongside requirements.txt when deploying on Postgres.
#   pip install -r requirements.txt -r requirements-postgres.txt
# Then set DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dreamlog
asyncpg>=0.30.0
